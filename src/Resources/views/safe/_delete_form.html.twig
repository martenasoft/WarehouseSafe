<form method="post" action="{{ path('safe_delete', {'id': safe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ safe.id) }}">
    <button class="btn">Delete</button>
</form>
