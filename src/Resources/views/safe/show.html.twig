{% extends 'base.html.twig' %}

{% block title %}Safe{% endblock %}

{% block body %}
    <h1>Safe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ safe.id }}</td>
            </tr>
            <tr>
                <th>Sum</th>
                <td>{{ safe.sum }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ safe.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ safe.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('safe_index') }}">back to list</a>

    <a href="{{ path('safe_edit', {'id': safe.id}) }}">edit</a>

    {{ include('safe/_delete_form.html.twig') }}
{% endblock %}
